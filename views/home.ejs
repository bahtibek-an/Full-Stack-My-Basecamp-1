<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <!-- <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/dropzone@5.9.2/dist/dropzone.css" /> -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;300;400;500;700&display=swap" rel="stylesheet">
    <style>
        span {
            color: red;
        }

        a {
            text-decoration: none;
            color: #000;
            font-family: "Roboto";
        }

        .h1-title span {
            font-weight: 400;
            font-family: "Roboto";
            color: #706F6F;
            line-height: 15px;
            font-size: 18px;
        }

        .h1-title {
            font-weight: 700;
            font-family: "Roboto";
        }

        .nav-title span {
            font-weight: 500;
            font-family: "Roboto";
            font-style: 30px;
            line-height: 20px;
            margin-left: 10px;
        }

        body {
            background: #ebe6e6d4;
        }
        ul{
            list-style: none;
            background: #ffff;
            border: 1px solid transparent;
            border-radius: 15px ;
            height: 450px;
        }
        li a{
            margin: 12px;
            color: #000;
        }
    </style>
</head>

<body>
    <% if (success==='true' ) { %>
        <script>
            setTimeout(function () {
                document.getElementById('successMessage').style.display = 'none';
            }, 5000);
        </script>
        <div id="successMessage" class="alert alert-success" role="alert">
            File uploaded successfully!
        </div>
        <% } %>

            <div class="container">
                <div class="d-flex justify-content-between my-3">
                    <h1 class="weight-300 nav-title">hello <span>
                            <%= username %>👋
                        </span></h1>
                    <div>
                        <button class="btn btn-warning"><a href="/">Log out</a></button>
                        <button class="btn btn-danger"><a href="/delete">delete account</a></button>
                    </div>
                </div>
                <div class="my-5">
                    <h2 class="weight-600 h1-title text-center my-4">Upload your file</span>
                    </h2>
                    <div class="apploadFolders d-flex align-items-center justify-content-center my-4">
                        <form action="/upload" method="post" enctype="multipart/form-data" class="d-flex align-items-center justify-content-center gap-5">
                            <div class="group d-flex align-items-center">
                                <!-- <label for="file">Choose your file</label> -->
                                <input type="file" name="file" class="form-control">
                            </div>
                            <div class="btn">
                                <button type="submit" class="btn btn-primary">Upload</button>
                            </div>
                        </form>
                    </div>
                   <div class="displayFile">
                    <% if (files && files.length > 0) { %>
                        <ul>
                          <% files.forEach(file => { %>
                            <li><a href="/uploads/<%= file %>"><%= file %></a></li>
                          <% }); %>
                        </ul>
                      <% } else { %>
                        <p>No files have been uploaded yet.</p>
                      <% } %>
                   </div>
                </div>
            </div>
</body>

</html>